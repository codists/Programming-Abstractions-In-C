/*题目
Define an enumeration type weekdayT whose elements are the days of the week.
Write function NextDay and PreviousDay that take a value of type weekdayT and 
return the day of the week that after or before the specified day, respectively.
For example，PreviousDay(Sunday) should return Saturday. Also write a function 
IncrementDay(startDay, delta) that return the day of the week that comes delta days
after startDay.Thus IncrementDay(Thursday, 4) should return Mondy,Your implementation
of IncrementDay should work if the value of delta is negative in which case it should
proceed backward in time.

*/